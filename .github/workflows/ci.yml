name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Run composer
      run: |
        composer create-project cakephp/app:4.1.0 test_app
        cd test_app
        bin/cake
