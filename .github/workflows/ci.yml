name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        repository: 'cakephp/app'
        ref: '4.1.0'
    - uses: actions/checkout@v1
    - name: Run composer
      run: |
        composer install
        bin/cake
